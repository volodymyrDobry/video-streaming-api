spring:
  data:
    mongodb:
      host: localhost
      port: 27017
      database: content_dev

logging:
  level:
    root: INFO
    com.viora: DEBUG

server:
  port: 8080

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,env,configprops
  endpoint:
    health:
      show-details: always

